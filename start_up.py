""" Start up script, run when installing program"""

# check if a mysql server is installed on the system
# if not installed ask the user do they want to install?
# if yes; install the mysql server(On Linux)


# create database and tables for now just username and password but will inclue other details such as;
# account creation time and date, gender, DOB